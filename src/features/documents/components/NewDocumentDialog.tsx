import { useState } from 'octane';
import { Button } from '../../../ui';
import { documentTemplates, type DocumentTemplate } from '../templates';
export function NewDocumentDialog({busy,onClose,onCreate}:{busy:boolean;onClose:()=>void;onCreate:(title:string,template:DocumentTemplate)=>void}) {
 const [title,setTitle]=useState('Untitled document'); const [selected,setSelected]=useState(documentTemplates[0]);
 return <div className="fixed inset-0 z-30 grid place-items-center bg-black/55 p-8 backdrop-blur-sm" role="dialog" aria-modal="true" aria-label="Create document"><section className="w-full max-w-2xl rounded-2xl border border-white/10 bg-[var(--surface-3)] p-5 shadow-2xl"><header><h2 className="text-sm font-semibold">Create a technical document</h2><p className="mt-1 text-xs text-zinc-500">Choose a useful structure and start editing immediately.</p></header><input value={title} onInput={e=>setTitle(e.currentTarget.value)} className="mt-5 w-full rounded-lg border border-white/10 bg-black/20 px-3 py-2.5 text-sm outline-none focus:border-lime-300/40" aria-label="Document title"/><div className="mt-4 grid grid-cols-2 gap-2">{documentTemplates.map(t=><button key={t.id} onClick={()=>setSelected(t)} className={`rounded-xl border p-3 text-left ${selected.id===t.id?'border-lime-300/30 bg-lime-300/[.08]':'border-white/[.07] bg-white/[.02] hover:bg-white/[.04]'}`}><div className="text-xs font-medium text-zinc-200">{t.name}</div><div className="mt-1 text-[11px] leading-4 text-zinc-500">{t.description}</div></button>)}</div><footer className="mt-5 flex justify-end gap-2"><Button onClick={onClose}>Cancel</Button><Button variant="primary" size="md" disabled={busy||!title.trim()} onClick={()=>onCreate(title.trim(),selected)}>Create document</Button></footer></section></div>;
}
